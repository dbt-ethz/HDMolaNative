using System;
using System.Collections.Generic;
using System.Text;

namespace Mola
{
    class FaceFilters
    {
        //public static bool[] AreaFilter(MolaMesh mesh, float area)
        //{
        //    bool[] result = new bool[mesh.FacesCount()];
        //    foreach (var f in mesh.Faces)
        //    {
        //        if(Fa)
        //    }
        //}
    }
}
